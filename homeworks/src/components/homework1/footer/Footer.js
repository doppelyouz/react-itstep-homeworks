import './footerStyles.scss';

const Footer = () => {
    return (
        <header className="footer">
            <div className="title">footer</div>
        </header>
    );
}

export default Footer;
