import './headerStyles.scss';

const Header = () => {
    return (
        <header className="header">
            <div className="title">header</div>
        </header>
    );
}

export default Header;